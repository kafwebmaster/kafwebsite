---
import HeaderSub from '../components/HeaderSub.astro';
import Menubar from '../components/Menubar.astro';
import Footer from '../components/Footer.astro';
import { getSiteData } from '../lib/content.js';
const data = await getSiteData();
// 理念文の {yearsRunning} / {fiscalYearLabel} を CMS の値で差し込む
// (「◯年目」「◯年度」を毎年書き換えずに済ませるため)
// replaceAll + 関数形式にしているのは、同じ差込みを 1 行に 2 回書いても
// 取りこぼさないため、および値に $& 等が含まれても置換パターンと解釈させないため
const fill = (s) => s
    .replaceAll('{yearsRunning}', () => data.edition.yearsRunning)
    .replaceAll('{fiscalYearLabel}', () => data.edition.fiscalYearLabel);
---
<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>KADOMA ART FESとは【KADOMA ART FES - 門真のまちをアートで染めよう】</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="大和田・古川橋駅前で行われる門真最大級アートイベントKADOMA ART FES">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/sub-page.css">
</head>
<style is:inline>
    body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
    }
    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
    }
    th, td {
        padding: 10px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }
    th {
        background-color: #f2f2f2;
    }
    .logo-container {
        text-align: center; /* 画像をセンターに寄せるために追加 */
    }
    .logo-container img {
        max-width: 70%; /* 画像の最大幅を70%に設定 */
        height: auto; /* 高さは自動調整 */
        display: block; /* 画像をブロック要素として扱い、センタリングするために追加 */
        margin: 0 auto; /* 左右のマージンを自動で設定してセンターに寄せる */
    }

	.kaf-event-details {
	    background-color: #fff; /* 背景を白くする */
	    padding: 20px; /* 内側の余白を追加 */
	    border-radius: 8px; /* 角を丸くする */
	    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* 影を追加 */
	    color: #000; /* 文字の色を黒にする */
	}
	.kaf-event-details .kaf-section-title, .kaf-event-details .kaf-sub-section-title {
	    font-size: 1.5em;
	    font-weight: bold;
	    margin-top: 20px;
	    margin-bottom: 10px;
	}
	.kaf-event-details table {
	    width: 100%;
	    border-collapse: collapse;
	    margin-bottom: 20px;
	}
	.kaf-event-details th, .kaf-event-details td {
	    padding: 10px;
	    text-align: left;
	    border-bottom: 1px solid #ddd;
	}
	.kaf-event-details th {
	    background-color: #f2f2f2;
	    color: #000; /* 文字の色を黒にする */
	}
	.kaf-event-details ul {
	    list-style-type: none;
	    padding: 0;
	}
	.kaf-event-details li {
	    padding: 10px;
	    border-bottom: 1px solid #ddd;
	    color: #000; /* 文字の色を黒にする */
	}
</style>
<body class="home">
    <div id="container">

        <!-- header(include) -->
        <HeaderSub />

        <!-- コンテンツを追加する場合は、以下のmainタグの中で作成してください。 -->
        <main>

            <section class="bg1">
            <section class="bg1">

                <h2 class="h2-right">
                    <span class="hosoku fade-in-text pc">KADOMAARTFES</span>
                    <span class="hosoku fade-in-text sp">KADOMA</span>
                    <span class="hosoku fade-in-text sp">ART</span>
                    <span class="hosoku fade-in-text sp">FES</span>
                    <span class="blur">門真市文化芸術<br class="sp">推進基本計画<br class="sp">パイロットプロジェクト<br class="sp">実行委員会</span>
                </h2>
    <div class="logo-container">
        <img src="images/KAF_logo02.png" alt="KAFLOGO">
    </div>
<p>
{data.siteText.about.map((line) => <>{fill(line)}{'\n'}</>)}
</p>
            </section>



<section class="kaf-event-details">
    <div class="kaf-section-title">
        <span>イベントの詳細</span>
    </div>
    <p>{data.siteText.intro}</p>
    <table>
        <tr>
            <th>名称</th>
            <td data-field="edition.displayName">{data.edition.displayName}</td>
        </tr>
        <tr>
            <th>メインイベント</th>
            <td>{data.siteText.mainEvent}</td>
        </tr>
        <tr>
            <th>開催日時</th>
            <td><span data-field="event.dateRangeLong">{data.event.dateRangeLong}</span> <span data-field="event.time">{data.event.time}</span></td>
        </tr>
        <tr>
            <th>開催場所</th>
            <td data-field="event.venue">{data.event.venue}</td>
        </tr>
        <tr>
            <th>入場料</th>
            <td data-field="event.admission">{data.event.admission}</td>
        </tr>
        <tr>
            <th>主催</th>
            <td><span data-field="organization.host">{data.organization.host}</span><br>門真市<br>NPO法人トイボックス</td>
        </tr>
    </table>
    <div class="kaf-sub-section-title">
        <span>全体コンテンツ</span>
    </div>
    <ul>
        {data.mmg.contents.map((item) => <li>{item}</li>)}
    </ul>
</section>




<Footer data={data} />
            </section>
        </main>

        <!--footer(include)-->

    </div>
    <!--/#container-->

    <!--ページの上部へ戻るボタン-->
    <div class="pagetop"><a href="#"><i class="fas fa-angle-double-up"></i></a></div>

    <!--開閉ブロック(include)-->
    <!-- メニュー内容を修正する場合は、includeHTML/menubar.htmlを修正してください。 -->
    <Menubar data={data} />

    <!--開閉ボタン（ハンバーガーアイコン）-->
    <div id="menubar_hdr_inner">
        <div id="menubar_hdr">
            <span></span><span></span><span></span>
        </div>
    </div>

    <!--jQueryの読み込み-->
    <script is:inline src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

    <!--lightbox用jsファイルの読み込み-->
    <script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.10.0/js/lightbox-plus-jquery.min.js"></script>

    <!--Swiper-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <script is:inline src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

    <!--スライドショー（slick）-->
    <script is:inline src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
    <script is:inline src="js/slick-thumbnail.js"></script>
    <script is:inline src="js/slick.js"></script>

    <!--パララックス（inview）-->
    <script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/protonet-jquery.inview/1.1.2/jquery.inview.min.js"></script>
    <script is:inline src="js/jquery.inview_set.js"></script>

    <!--このテンプレート専用のスクリプト-->
    <script is:inline src="js/main.js"></script>

    <!--ロゴアニメーション-->
    <script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/vivus/0.4.6/vivus.min.js"></script>
    <script is:inline>
        new Vivus('svg-logo', {
            start: 'autostart',
            type: 'scenario-sync',
            duration: 150,
            forceRender: false,
        });
    </script>


</body>

</html>