# Claude Code 指示テンプレート集

このファイルは、各作業ステップで Claude Code に渡す指示文のテンプレート集です。
コピー&ペーストしてそのまま使うか、状況に応じて微調整してください。

---

## 【最重要】初回起動時の指示

Claude Code を起動した一番最初に、以下をそのまま貼り付けてください。

````
あなたはこのプロジェクト(KADOMA ART FES 公式サイト)のリファクタリング作業を担当します。
以下の手順で進めてください。

## ステップ 1: プロジェクトの把握

まず、以下のファイルをこの順序で読み、内容を理解してください。

1. `CLAUDE.md` — プロジェクトの全体方針、制約、スキーマ
2. `docs/refactoring-plan.md` — 作業手順
3. `docs/prompts.md` — 指示テンプレート集(参考用)

次に、現行サイトの構造を把握するため、以下を確認してください。

- ルートディレクトリの構成(`ls` レベルで十分)
- `index.html` の全体
- `kaf_mmg.html`, `kaf_marche.html`, `contest_entry.html`, `kaf_support.html` の全体
- `includeHTML/` 配下のすべてのファイル
- `js/include.js`, `js/main.js`
- `css/style.css` は読まなくて良い(触らない方針のため)

## ステップ 2: 現状の理解を報告する

上記を読み終えたら、以下の項目について理解した内容を私に報告してください。

1. プロジェクトの性質と目的(1〜2 文で)
2. 今回の作業の目的(1〜2 文で)
3. 厳守すべき制約のうち、特に注意すべきと感じたもの上位 3 つ
4. 改修対象ファイルの一覧(優先順)
5. 作業を進める上で気になった点・曖昧な点・事前に確認したい点があれば列挙

ここで重要なのは 5 番です。指示書に書かれていないケース、矛盾して見える箇所、
現行コードを読んで初めて気付いた問題点があれば、作業を始める前に必ず指摘してください。
例えば以下のような点です:

- data.json のスキーマに含まれていないが、HTML 側で年度依存と思われる値
- 複数の HTML ファイル間で値が食い違っている箇所
- 既存コードのバグや不整合(例: alt="KAF2024" が KAF5 のページに残っている等)
- スキーマに定義されたキーと、実際の HTML の値が対応付かない箇所

## ステップ 3: 作業計画の提示

次に、フェーズ 1-1(data.json の初期作成)に取り組む前の作業計画を提示してください。

- どのファイルからどの値を抽出するか
- 表記揺れがある値をスキーマのどのキーに振り分けるか
- 現時点で不明・要確認な値があれば明示

計画は具体的に書いてください。抽象的な「慎重に進めます」ではなく、
「index.html の 56 行目から開催日時を抽出し、event.dateRange に入れます」のように、
行番号レベルで示してください。

## ステップ 4: 私の承認を待つ

ステップ 2 と 3 を提示したら、そこで一度止まってください。
私が内容を確認し、「進めてください」と返すまで、
ファイルの作成・編集・シェルコマンドの実行は一切行わないでください。

---

## 作業中の基本姿勢

以降の作業を通じて、以下を守ってください。

- 小さく刻む: 1 プロンプト 1 ファイル、あるいは 1 プロンプト 1 機能を原則とする
- 差分を見せる: ファイル編集前に必ず diff を提示し、承認を得てから書き込む
- 疑問があれば止まる: 指示が曖昧に感じた場合、推測で進めず必ず確認する
- 制約を最優先: CLAUDE.md に書かれた「厳守すべき制約」に少しでも触れる可能性がある変更は、必ず事前に相談する
- デザインを壊さない: 今回の作業で見た目が変わってはいけない。変わるのは値の注入方法だけ
- 日本語原文を尊重: HTML 内の日本語テキストを data.json に移植する際、一字一句そのまま転記する

それでは、ステップ 1 から始めてください。
````

---

## フェーズ 1-1: data.json の初期作成

```
CLAUDE.md に記載された data.json スキーマに従い、
data.json を新規作成してください。

値は現行の HTML ファイル(index.html, kaf_mmg.html, kaf_marche.html,
contest_entry.html, kaf_support.html, includeHTML/menubar.html)を
実際に読み取り、そこに書かれている原文を一字一句そのまま転記してください。

原文が複数ファイルで表記揺れしている場合(例: "2026年3月7日" と "2026年03月07日")は、
スキーマで用意した別キー(dateRange と dateRangeLong など)にそれぞれ振り分けてください。

表記を勝手に統一したり翻訳したりしないでください。

作成前に、各ファイルから抽出した値のリストを私に見せて確認を取ってください。
```

---

## フェーズ 1-2: include.js の Promise 化

```
js/include.js を Promise 化してください。

現在の実装は $(function(){}) 内で 4 つの .load() を直列に呼んでいるだけですが、
以下の要件を満たすように書き換えてください:

1. $.get() でそれぞれの共通パーツを取得し、取得後に該当要素に html() で挿入
2. $.when() で 4 つの完了を待ち合わせ
3. 完了後、document に対して 'includes:loaded' カスタムイベントを発火
4. 既存のセレクタ(#include-header, #include-menubar, #include-hbpinfo, #include-footer)は変更しない
5. 失敗時のエラーハンドリングも追加(console.error で通知)

書き換え前に差分を見せてください。
```

---

## フェーズ 1-3: data-loader.js の作成

```
js/data-loader.js を新規作成してください。

要件:
1. jQuery ベースで実装
2. document の 'includes:loaded' イベントを待ってから処理開始
3. $.getJSON('data.json') で data.json を取得
4. 以下の 4 つの属性に対応:
   - data-field="パス"       → 要素の text を差し込み
   - data-src-field="パス"   → 要素の src 属性を差し込み
   - data-href-field="パス"  → 要素の href 属性を差し込み
   - data-news-list          → news 配列を <dt>/<dd> ペアで展開
5. パスはドット区切り記法に対応(例: "event.dateRange" → data.event.dateRange)
6. 値が見つからない場合は元のテキストを残す(警告は console.warn で出す)
7. JSON 取得失敗時は console.error で通知し、処理を中断

コードにはコメントを適度に入れてください。
作成後、動作確認用に使える最小の HTML スニペットも併せて提示してください。
```

---

## フェーズ 2-1: index.html の最小検証

```
index.html の「開催日時」を表示している 1 箇所だけを data-field 方式に置換してください。
他の箇所は一切変更しないでください。

併せて、index.html の </body> 直前にある既存の <script> 群の後ろに、
<script src="js/data-loader.js"></script> を追加してください。

変更前に差分を見せてください。
```

---

## フェーズ 2-2: index.html 全体の改修

```
index.html の年度依存情報をすべて data-field 方式に置換してください。

対象:
- イベント詳細(開催日時、場所、入場料)
- フライヤー画像(<img src="...">)
- お知らせ欄(<dl class="news">)全体を data-news-list 方式に置換
- 古い alt="KAF2024" のような取り残された記述があれば、汎用的な日本語に修正

対象外(触らない):
- CSS クラス名
- 既存のレイアウト構造
- アニメーション関連のクラス(fade-in-text, blur 等)
- セクションの見出しテキスト(恒久的な文言なので data 化不要)

変更前に、置換対象の一覧と新しいマークアップ案を見せて確認を取ってください。
```

---

## フェーズ 3: 他ページへの横展開(1 ファイルずつ)

```
次は {対象ファイル名} を改修してください。

index.html で採用したのと同じ方針で、年度依存情報を data-field 方式に置換します。
新しい data-field キーが必要になった場合は、先に data.json のスキーマ追加を提案してください。

{対象ファイル名} 特有の注意点:
- (ここにファイル固有の注意点を書く。例えば kaf_marche.html なら
  「現在 2024 年の情報のまま放置されているので、data.json の KAF5 情報に正常化する」など)

変更前に、置換対象の一覧と新しいマークアップ案を見せてください。
```

---

## フェーズ 4: 画像整理

```
画像を images/editions/kaf5/ フォルダ構成に整理してください。

1. images/editions/kaf5/ フォルダを作成
2. 以下の対応で既存画像をコピー(移動ではなくコピー、旧ファイルは残す):
   - images/KAF5_flyer_omote.jpg → images/editions/kaf5/flyer_front.jpg
   - images/KAF5_flyer_ura.jpg   → images/editions/kaf5/flyer_back.jpg
   - images/KAF5_flyer.jpg       → images/editions/kaf5/flyer_full.jpg
   - images/KAF5.jpg             → images/editions/kaf5/main_visual.jpg
   - images/KAF5_support.jpg     → images/editions/kaf5/support_list.jpg
   - images/KAF5web_banner2.jpg  → images/editions/kaf5/web_banner.jpg
   - images/KAF5entry-img.png    → images/editions/kaf5/entry_thumbnail.png
3. 完了後、data.json の images セクションが新パスを指していることを確認

画像ファイルのコピーはシェルコマンド(cp)で行ってください。実行前にコマンドを提示してください。
```

---

## フェーズ 7: 運営スタッフ向けマニュアルの作成

```
docs/update-guide.md を作成してください。

対象読者: プログラミング知識のない実行委員会スタッフ(Word と Excel は使える程度)

内容:
- 次回大会(KAF6)への更新時にやること
- data.json のどこを書き換えるか(具体例付き)
- 新しい画像をどこにどう配置するか
- お知らせを追加するにはどうするか
- GitHub でのコミット手順(クリック操作レベルで)
- 本番反映の方法(FTP)
- よくあるトラブルと対処法(JSON の文法エラー、画像が表示されない、など)

専門用語は極力避け、避けられない場合は注釈を入れてください。
スクリーンショットが必要な箇所は「ここにスクリーンショット」とプレースホルダを入れておいてください。
```

---

## 汎用: 何か詰まったとき

```
今の作業で詰まっています。状況は以下の通りです:
- やろうとしていること: {...}
- 実行した操作: {...}
- 期待した結果: {...}
- 実際に起きたこと: {...}

原因を特定し、次の一手を提案してください。
いきなりコードを書き換える前に、まず原因分析を聞かせてください。
```
